#HTTP Server
server:
  port: 1111

#Discovery Server Access
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    status-page-url-path: /infoAlumno
    health-check-url-path: /healthAlumno
    metadata-map:
       app-owner: rjrc
       description: course microservice
